/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "placehold.co","firebasestorage.googleapis.com"],
  },
};

module.exports = nextConfig;
